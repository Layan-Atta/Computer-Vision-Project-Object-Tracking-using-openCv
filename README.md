# مشروع الرؤية الحاسوبية: تتبع الأجسام

هذا المشروع هو تطبيق عملي لمفاهيم الرؤية الحاسوبية كجزء من التدريب لشركة "الأساليب الذكية".

المشروع يغطي مهمتين أساسيتين من قائمة المهام المطلوبة:
1.  **Object Tracking** باستخدام مكتبة `OpenCV`.
2.  **Object Tracking** باستخدام حساس `HuskyLens` (تم توثيق الخطوات نظرياً).

---

## 🎯 المهمة الأولى: تتبع الأجسام باستخدام OpenCV

تم تنفيذ هذه المهمة بالكامل باستخدام **Python** ومكتبة **OpenCV** على منصة **Google Colab**.

* **الهدف:** تتبع جسم بلون معين في مقطع فيديو.
* **الفيديو المستخدم:** تم استخدام مقطع فيديو مدته 15 ثانية من [Animated Moving Blue Glass Balls](https://www.youtube.com/watch?v=T_PYhcHp0JE) نظراً لوضوح الألوان وثباتها، مما يجعله مثالياً لاختبار تتبع الألوان.

![v](output_converted2.gif)


### خطوات العمل

1.  **إعداد البيئة:** تم رفع ملف الفيديو (`.mp4`) إلى بيئة Google Colab.
2.  **تحويل نظام الألوان:** استخدم الكود مكتبة `cv2` لقراءة الفيديو إطاراً تلو الآخر. تم تحويل كل إطار من نظام ألوان `BGR` (الافتراضي في OpenCV) إلى نظام `HSV` (Hue, Saturation, Value)، لأن نظام `HSV` أسهل في عزل الألوان بغض النظر عن ظروف الإضاءة.
3.  **تحديد نطاق اللون:** تم تحديد نطاق اللون "الأزرق" (لون "بيبي شارك") لتعقبه.
    * الحد الأدنى للون الأزرق: `[100, 150, 50]`
    * الحد الأعلى للون الأزرق: `[140, 255, 255]`
4.  **إنشاء القناع (Mask):** تم استخدام دالة `cv2.inRange` لإنشاء "قناع" (صورة أبيض وأسود) يعزل جميع البكسلات التي تقع ضمن النطاق الأزرق المحدد.
5.  **إيجاد الجسم (Contours):** باستخدام دالة `cv2.findContours`، تم تحديد الخطوط الخارجية لأكبر كتلة لونية (أكبر جسم أزرق) في القناع.
6.  **رسم المربع:** بعد إيجاد أكبر جسم، تم حساب إحداثيات المستطيل المحيط به (`cv2.boundingRect`) ورسم مستطيل أخضر حوله مباشرة على الإطار الأصلي للفيديو.
7.  **تصدير النتيجة:** تم حفظ الفيديو الجديد (الذي يحتوي على المربعات الخضراء المتتبعة) باسم `output_tracking_video.mp4`.

### 🎥 النتيجة


![out](output_converted.gif)
---

## 🧩 المهمة الثانية: تتبع الأجسام باستخدام HuskyLens (خطوات نظرية)

**ملاحظة:** هذه الخطوات هي شرح نظري لكيفية تنفيذ المهمة حضورياً باستخدام قطعة **HuskyLens** (كما تم شرحها في فيديو التدريب)، وذلك لعدم توفر الأدوات حالياً.

الـ **HuskyLens** هي قطعة (حساس) ذكاء اصطناعي جاهزة تحتوي على كاميرا وشاشة ومعالج لتنفيذ مهام الرؤية الحاسوبية بسهولة وبدون الحاجة لكتابة كود معقد.

### خطوات التنفيذ النظرية

1.  **التشغيل والتوصيل:** يتم توصيل قطعة HuskyLens بمصدر طاقة (مثل بطارية أو حاسب عبر منفذ USB).
2.  **اختيار النمط (Mode):** باستخدام "الزر الدوار" (Function Button) الموجود على جانب القطعة، يتم التنقل بين الأنماط (مثل: Face Recognition, Line Tracking...). نختار نمط **"Object Tracking"** (تتبع الأجسام).
3.  **"تعليم" القطعة (Learning):**
    * نوجه كاميرا الـ HuskyLens نحو الجسم الذي نريد تتبعه (مثلاً: "بيبي شارك" الأزرق على شاشة الجوال).
    * نضغط ضغطة قصيرة على "زر التعلم" (Learning Button) الموجود في الأعلى.
    * سيظهر مربع أصفر على الشاشة، نوجهه ليحيط بالجسم المراد تتبعه.
    * نضغط ضغطة طويلة على "زر التعلم" لحفظ الجسم. ستظهر رسالة على الشاشة (مثل: "Learning: ID1") لتأكيد حفظ الجسم في الذاكرة.
4.  **بدء التتبع:** بمجرد حفظ الجسم، ستقوم الـ HuskyLens بتتبعه تلقائياً أينما تحرك أمام الكاميرا، وستعرض مربعاً ملوناً حوله على شاشتها في الوقت الفعلي.
5.  **(اختياري) ربطها بالروبوت:** إذا كان الهدف هو بناء روبوت يتبع الجسم، يمكن توصيل الـ HuskyLens بمتحكم دقيق (مثل الأردوينو) عبر منافذ (UART) لقراءة بيانات التتبع (مثل إحداثيات X و Y للجسم) واستخدامها لتوجيه محركات الروبوت.
